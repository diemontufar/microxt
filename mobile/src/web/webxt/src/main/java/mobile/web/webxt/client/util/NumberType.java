package mobile.web.webxt.client.util;

public enum NumberType {
	
	TEXT,		// for normal InputBox used in forms too
	DECIMAL,	// for numerical field with format
	CURRENCY,   // for currency field with format
	DATE,		// for date field with format
	INTEGER		// for integer field without format

}
